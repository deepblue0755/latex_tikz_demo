\documentclass[tikz,border=10pt]{standalone}
\usepackage{CJKutf8}
\usepackage[utf8]{inputenc}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning}

\begin{document}
\begin{CJK*}{UTF8}{gbsn}
\begin{tikzpicture}[
    box/.style={
        draw,
        rounded corners,
        minimum width=5.8cm,
        minimum height=1.1cm,
        align=left,
        font=\small
    },
    arrow/.style={
        ->,
        thick,
        >=Stealth
    },
    node distance=1.6cm and 2.2cm
]

% 第一行（左 → 右）
\node[box] (step1) {1. 检查系统需求};
\node[box, right=of step1] (step2) {2. 搭建编译环境};
\node[box, right=of step2] (step3) {3. 选择设备};
\node[box, right=of step3] (step4) {4. 下载源代码};

% 第二行（右 → 左）
\node[box, below=2cm of step4] (step5) {5. 系统定制};
\node[box, left=of step5] (step6) {6. 编译与打包};
\node[box, left=of step6] (step7) {7. 烧录并运行};

% 箭头连接（蛇形）
\draw[arrow] (step1) -- (step2);
\draw[arrow] (step2) -- (step3);
\draw[arrow] (step3) -- (step4);

\draw[arrow] (step4) -- (step5); % 向下

\draw[arrow] (step5) -- (step6);
\draw[arrow] (step6) -- (step7);


\end{tikzpicture}

\end{CJK*}
\end{document}
